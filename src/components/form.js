import React, { Component } from 'react';
import { View, Text } from 'react-native';

class Form extends Component {
    render() {
        return(
            <View style={{flex: 1}}>
            </View>
        );
    }
}

export { Form };
